sudo: false
env:
  - TEST_SHIELD=true LS_USER=user LS_PASSWORD=changeme
before_install:
  - bash -x ssl_integration.bash
language: ruby
cache: bundler
rvm:
  - jruby-1.7.23
script: bundle exec rspec spec --tag elasticsearch_secure
