sudo: false
env:
  - INTEGRATION=false TEST_DEBUG=1
  - INTEGRATION=true TEST_DEBUG=1 ES_VERSION=2.2.0
  - INTEGRATION=true TEST_DEBUG=1 ES_VERSION=1.7.5
language: ruby
cache: bundler
rvm:
  - jruby-1.7.24
script: ./travis-run.sh
